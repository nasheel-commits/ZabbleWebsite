# Brief 06 — How to automate bank & POS reconciliation

> **Owner:** S06 content · **Type:** Cluster (Reconciliation) · **Wave:** 1, Week 6
> **Status:** ready to write · **Voice:** problem → what we build → what changes

| Field | Value |
|-------|-------|
| **Canonical target page** | `/systems/reconciliation-engine` (money page — owned by S02 template; we LINK to it) |
| **Article URL** | `/guides/automate-bank-reconciliation` (NEW blog/cluster — S04 to confirm path) |
| **Cluster (keyword-map)** | Reconciliation |
| **Primary query** | "how to automate bank reconciliation" / "automated bank reconciliation" |
| **Secondary** | "reconciliation software", "POS vs bank reconciliation", "automate POS reconciliation" |
| **Search intent** | Informational/how-to (article) — distinct from money page's commercial "reconciliation software south africa" |
| **Layer** | AEO + SEO |
| **ZA/global** | ZA-core; broader-Africa upside |
| **Rubric** | B5 W4 F2 D2 E5 → **3·5+2·4+2·2+2·2+1·5 = 36 → P1** |
| **Word count** | 1,400–1,800 |
| **Evidence** | `automated bank reconciliation` 10/mo (commercial, MEDIUM), `reconciliation software` 10/mo (LOW, CPC $10.12). `serp-organic__automated-bank-reconciliation__za.json`: **AI Overview + PAA present**. PAA captured (see FAQ). |

## Cannibalisation guard
Article = how-to/informational. Money page `/systems/reconciliation-engine` =
commercial. Different intent, different URL → no overlap; the article funnels to
the page. S02 sets canonical on the money page; S05 confirms primary keywords differ.

## Answer-first intro spec
*"Yes — bank and POS reconciliation can be automated. A reconciliation engine
ingests each ledger as it lands (POS, bank, accounting, processor), auto-matches
the easy lines in seconds, and surfaces only the mismatches that need a human.
The weekly spreadsheet that chased agreement between three sources of truth
stops being a job."* Then the arc.

## H2/H3 outline (question-led)
- **H2 — Can you automate bank reconciliation?** *(PAA)* (yes; 2-sentence answer first)
- **H2 — Manual vs automated reconciliation: what changes?** *(PAA)*
- **H2 — How automated reconciliation works (step by step)**
  - H3 — Ingest every ledger as it lands
  - H3 — Auto-match the easy cases
  - H3 — Surface only real exceptions to a human
  - H3 — Learn the resolution so it auto-clears next time
- **H2 — POS vs bank vs processor vs inventory — same engine, different ledgers**
- **H2 — Can AI do a bank reconciliation?** *(PAA — honest framing: rules + learned resolutions)*
- **H2 — What changes** (ten thousand lines → a handful of exceptions; finance does decisions, not data-entry)
- **H2 — FAQ**

## Required statistics / sources (GEO)
- 1 stat on time spent on manual reconciliation / close (cite + year).
- Internal proof: "ten thousand lines → a handful of real exceptions".
- **Expert-quote slot:** Zabble lead on exception-only finance ("only the mismatches that need a human ever reach the queue").

## FAQ (AEO — seeded from live SA PAA)
1. **Can you automate bank reconciliation?** *(PAA)*
2. **What is the difference between manual and automated bank reconciliation?** *(PAA)*
3. **What are the 5 steps for bank reconciliation?** *(PAA)*
4. **Can AI do a bank reconciliation?** *(PAA)*
5. **Does it work with my POS and bank feed?**

## Internal links
- **Up →** P2 business-process-automation hub.
- **Across (money) →** `/systems/reconciliation-engine` (outcome anchor).
- **Siblings →** brief 07 (compliance), Wave-2 accounting-engine.
- **CTA →** `/diagnose`.

## CTA
*"How many hours does reconciliation eat each month? We'll show you the version
where it's minutes."* → `/diagnose`.
