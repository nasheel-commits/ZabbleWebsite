# ─────────────────────────────────────────────────────────────────────────────
# Zabble SEO/AEO/GEO — environment variable template.
# Copy this file to `.env` and fill in real values. NEVER commit `.env`.
# `.env` and `.env.*` are git-ignored (see .gitignore); only this `.env.example`
# (no secrets) is tracked. See docs/seo/00-access-and-credentials.md.
# ─────────────────────────────────────────────────────────────────────────────

# DataForSEO API credentials.
# USERNAME = your DataForSEO API login (the account email).
# PASSWORD = the API password shown in the dashboard "API CREDENTIALS" block
#            at https://app.dataforseo.com/api-access  — this is NOT your
#            website/login password. Auth is HTTP Basic (username:password).
DATAFORSEO_USERNAME=you@example.com
DATAFORSEO_PASSWORD=your_api_password_here

# Basic-auth token for the HOSTED MCP server (https://mcp.dataforseo.com/http).
# It is base64("username:password"). Generate it with:
#   printf '%s' "$DATAFORSEO_USERNAME:$DATAFORSEO_PASSWORD" | base64
# Referenced by .mcp.json as ${DATAFORSEO_BASIC_AUTH} (so the token never lands
# in git). Regenerate whenever the API password changes.
DATAFORSEO_BASIC_AUTH=base64_of_username_colon_password

# Which DataForSEO MCP tool groups Claude Code may use. COMMA-separated, no spaces.
# Valid: AI_OPTIMIZATION, SERP, KEYWORDS_DATA, ONPAGE, DATAFORSEO_LABS, BACKLINKS,
#        BUSINESS_DATA, DOMAIN_ANALYTICS, CONTENT_ANALYSIS, MERCHANT.
# Omit the variable entirely to enable ALL modules.
ENABLED_MODULES=SERP,KEYWORDS_DATA,DATAFORSEO_LABS,ONPAGE,BACKLINKS,DOMAIN_ANALYTICS,AI_OPTIMIZATION,CONTENT_ANALYSIS

# Free sandbox base URL — returns dummy data, never billed. Used for wiring/CI
# checks. Live base URL is https://api.dataforseo.com (billed per call).
DATAFORSEO_SANDBOX_URL=https://sandbox.dataforseo.com

# ─────────────────────────────────────────────────────────────────────────────
# Measurement & indexing (Session 09 — analytics).
# These map to runtimeConfig.public.analytics (see nuxt.config.ts). They are
# NOT secrets (a GTM/GA4/Clarity id is exposed in client HTML by design), but we
# keep them in env so staging and production can differ and ids stay out of code.
# An EMPTY id ⇒ that tag never loads. See docs/seo/id-secret-registry.md.
# ─────────────────────────────────────────────────────────────────────────────

# Google Tag Manager web container (primary). Format: GTM-XXXXXXX.
NUXT_PUBLIC_ANALYTICS_GTM_ID=

# GA4 Measurement ID. Format: G-XXXXXXXXXX.
# Only used directly when NO GTM container is set; otherwise GA4 is configured
# INSIDE the GTM container. (GA4 timezone Africa/Johannesburg + currency ZAR are
# GA4 property settings in the GA4 UI, not env.)
NUXT_PUBLIC_ANALYTICS_GA4_ID=

# Microsoft Clarity project id (consent-gated, loaded only after opt-in).
NUXT_PUBLIC_ANALYTICS_CLARITY_ID=

# Verbose client-side tracing of consent + events: true | false.
NUXT_PUBLIC_ANALYTICS_DEBUG=false

# Search-engine site verification (in-code fallback to DNS TXT). Rendered as
# <meta> into prerendered HTML by app/plugins/seo-verification.ts. Empty ⇒ no tag.
# GSC "HTML tag" method (the content="" value from the google-site-verification meta):
NUXT_PUBLIC_VERIFICATION_GOOGLE=
# Bing Webmaster "Add a meta tag" method (the msvalidate.01 content value):
NUXT_PUBLIC_VERIFICATION_BING=

# IndexNow key (PUBLIC — also hosted at https://zabble.org/<key>.txt). Used by
# scripts/indexnow-ping.mjs. The committed default lives in public/<key>.txt;
# set this only to override (e.g. rotate the key).
NUXT_INDEXNOW_KEY=

# Production site origin (used by scripts/indexnow-ping.mjs to scope/guard URLs).
NUXT_PUBLIC_SITE_URL=https://zabble.org

# ─────────────────────────────────────────────────────────────────────────────
# Discovery-call booking (/api/book) — Google Calendar + Meet + Gmail.
# Powers the /diagnose lead form. Until these are set the form still works and
# shows the pain profile, but falls back to a mailto instead of auto-booking.
# Set the same vars in Vercel (Settings → Environment Variables).
# Full setup walkthrough: docs/booking-setup.md
# ─────────────────────────────────────────────────────────────────────────────

# Service-account email from the Google Cloud JSON key (client_email field).
NUXT_GOOGLE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com

# Service-account private key (private_key field from the JSON key).
# Keep it on ONE line with literal \n in place of real newlines, wrapped in quotes.
NUXT_GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEv...\n-----END PRIVATE KEY-----\n"

# The Workspace mailbox whose calendar receives bookings. The service account
# impersonates this address (domain-wide delegation).
NUXT_GOOGLE_IMPERSONATED_USER=sales@zabble.org

# Where the lead-notification email is sent (usually the same as above).
NUXT_SALES_NOTIFY_EMAIL=sales@zabble.org

# IANA timezone the booking time-slots are offered in.
NUXT_BOOKING_TIMEZONE=Africa/Johannesburg

# Length of the discovery call, in minutes.
NUXT_BOOKING_DURATION_MINUTES=30
